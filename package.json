{
  "name": "multi-model-router",
  "version": "1.0.0",
  "description": "Per-stage multi-LLM routing with cost/quality metadata and token gate",
  "main": "dist/example.js",
  "scripts": {
    "demo": "ts-node src/example.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
